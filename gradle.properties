org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
kotlin.code.style=official
# iOS targets are declared in shared/build.gradle.kts but can only be built on a macOS
# host. This silences the "disabled target" warning when building on Windows/Linux.
kotlin.native.ignoreDisabledTargets=true
